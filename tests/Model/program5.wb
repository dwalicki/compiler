/* program5.wb

Wabbit has conditionals.  Here is a program that prints out the
minimum value of two variables.
*/

var a int = 2;
var b int = 3;
var minval int;
if a < b {
   minval = a;
} else {
   minval = b;
}
print minval;   // Should print 2
