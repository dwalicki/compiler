/* 11_term.wb

   Multiplicative terms in expressions.  These are terms
   that consist solely of multiply/division operators.
   Expressions should associate left-to-right meaning
   that a*b*c gets structured as ((a*b)*c) when you
   build the model.
*/

print 1*2;
print x*y;
print 1/2;
print x/y;
print 1*2*3/x*y;
