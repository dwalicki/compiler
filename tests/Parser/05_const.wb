/* 05_const.wb

   Parse a single const statement. A tricky thing here is that
   a const can have an optional type.   Your parsing rule will
   need to peek ahead to know if there is a type or not.
*/

const a = 1;
const a int = 1;


